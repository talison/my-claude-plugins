# my-claude-plugins

Personal Claude Code plugin marketplace. Forks of upstream plugins with custom improvements.

- `telegram` — fork of `anthropics/claude-plugins-official/external_plugins/telegram`, backed by nanoclaw's shared `telegram-core` (see `plugins/telegram/core/.sync-source`).

See `docs/UPSTREAM.md` for how we track upstream changes.
